var D4, chai, sinon;

chai = require('chai');

sinon = require('sinon');

D4 = require('../src/d4').D4;

chai.should();

describe('d4', function() {
  return it('should do something', function() {
    return true.should.equal(true);
  });
});
